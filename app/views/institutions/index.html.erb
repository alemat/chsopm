    <div class="header">
      <h2>
        Civil Society Organizations
      </h2>
    </div>
    <div class="body table-responsive">
      <table class="table table-bordered table-striped table-hover dataTable js-exportable">
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>CSO type</th>
      <th>CSO status</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @institutions.each do |institution| %>
      <tr>
        <td><%= institution.id %></td>
        <td>
          <%= link_to institution do %>
            <%= image_tag institution.image, size: '32x32', class: 'img-profile' rescue nil %> 
          <% end %> <%= institution.short_name %></td>
        <td><%= institution.institution_type %></td>
        <td><%= institution.institution_status %></td>
        <td><%= link_to 'Details', institution, class: 'btn btn-primary' %>
        <%= link_to 'Edit', edit_institution_path(institution), class: 'btn btn-primary' %>
          
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Add New CSO', new_institution_path, class: 'btn btn-primary' %>
</div>
