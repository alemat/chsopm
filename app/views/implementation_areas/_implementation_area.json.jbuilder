json.extract! implementation_area, :id, :project_id, :region_id, :zone, :district, :contact_person, :phone_number, :email, :created_at, :updated_at
json.url implementation_area_url(implementation_area, format: :json)
