<div class="body">

<p>
  <strong>Name:</strong>
  <%= @institution_type.name %>
</p>

<%= link_to 'Edit', edit_institution_type_path(@institution_type), class: 'btn btn-primary' %> |
<%= link_to 'Back', institution_types_path, class: 'btn btn-primary' %>
</div>